# Botola Go Admin — environment variables
# All client-exposed vars must be prefixed with VITE_

VITE_APP_NAME="Botola Go"
VITE_API_BASE_URL="https://api.botolago.app/v1"
# Use mock data instead of a real backend (frontend-only mode)
VITE_USE_MOCKS="true"
